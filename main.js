function add (left, right) {
    return left + right;
}

module.exports = add;

fuction printmultiplytable(generateMutiplyArray)
fuction generateMultiplyArray(firstNumber,secondNumber)
var generatedarray=[];
var times=secondnumber-firstNumber+1;
for (let rowindex=0;row<times;row ++){
    var row=[]
for (let rowindex=0;row index<times;rowindex++)
fuction isvaild (firstNumber,secondNumber)

fuction multiplytable(firstNumber,secondNumber)
var output=""
var valid=isvalid(firstNumber,secondNumber)
if(valid){
    return valid
}

var multiply=generateMutiplyArray(firstNumber,secondNumber)
output=printmutiply